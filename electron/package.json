{
  "name": "ids-vote",
  "version": "1.0.0",
  "description": "IDS Vote Electron App",
  "author": "ollejah@gmail.com",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "start": "NODE_ENV=production electron .",
    "dev": "NODE_ENV=development electron .",
    "release": "electron-packager . IDS Vote --platform=darwin --arch=x64 --app-version=$npm_package_version --out=release --overwrite --icon=icon.icns",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "electron": "^2.0.4",
    "electron-packager": "^12.1.0"
  }
}
